#include "libft.h"

int	main(void)
{
	ft_isalpha("Aa");
	ft_isdigit(1223);
	ft_isalnum
	ft_isascii();
	ft_isprint();
	ft_strlen("a");
	ft_memset(str + 13, '.', 8*sizeof(char));
	printf("memset: %s", str);

	return (0);
}
